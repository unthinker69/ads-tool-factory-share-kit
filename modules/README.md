# modules 可复用模块

模块是介于 core 和 product 之间的复用层。

- `score-control/`：评分、重试、高分候选池。
- `provider-pool/`：模型池、agent 分配、并发策略。

后续可以新增：

- `google-app-campaign/`
- `meta-ads/`
- `tiktok-ads/`
- `social-content-app/`
- `ecommerce/`
- `game/`

